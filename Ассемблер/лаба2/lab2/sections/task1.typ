#v(0.3cm)

#set par(justify : true, first-line-indent: 2em)

Первая цифра задана в AX, вторая цифра задана в BX. Написать программу, которая выводит в одну строку первую цифру, пробел, вторую цифру.

#align(center)[#image("task1.png")]
#text(size: 12pt, align(center)[Фото запуска первой программы])

```nasm
.model tiny  
.code                     
org 100h                  
                         
start:
  ; печать фамилии, имени и номера группы
  mov AH,09h
  mov DX, offset Names
  int 21h
  
  ; переход на следующую строку
  call enter

  ; вывод цифр
  mov AX, 7    ; занесение первой цифры в регистр AX
  ADD AX, 30h  ; перевод цифры в символьную форму с помощью кода ASCII и команды ADD
  mov BX, 5    ; занесение второй цифры в регистр BX
  ADD BX, 30h  ; перевод цифры в символьную форму с помощью кода ASCII и команды ADD
  
  ; вывод первой цифры
  mov DX, AX
  mov AH, 02h
  int 21h

  ; вывод пробела 
  mov DL, 00h   ;код пробела из ASCII
  int 21h 

  ; вывод второй цифры 
  mov DX, BX
  mov AH, 02h
  int 21h 

  ; завершение программы
  mov AX, 4C00h
  int 21h
  
  ; переход на следующую строку
  enter proc
    mov DX, offset nline
    mov AH, 09h
    int 21h
    ret
  enter endp


Names db 'Grigoriev Danya 251$'
nline db 0Dh,0Ah,'$'
end start
```
#text(size: 12pt, align(center)[Код первой программы])

#pagebreak()

#set text( 
  size: 11pt
)

#set table.hline(stroke: .6pt)

#table(
  // set text(1pt),
  columns: 13,
  stroke: none,
  inset: 2.5pt,
  align: center,
  table.hline(),
  table.header(
    [*Шаг*], 
    [*Машинный 
    \код*], 
    [*Команда*], 
    table.cell(colspan: 9, 
      
      [*Регистры*]
    ),
    [*Флаги*],
    table.hline(start: 3),
    [ ], [ ], [ ], [AX], [BX], [CX], [DX], [SP], [DS], [SS], [CS], [IP], [CZSOPAID],
  ),
  
 
  table.hline(),
  table.vline(x: 0),
  table.vline(x: 1),
  table.vline(x: 2),
  table.vline(x: 3),
  table.vline(x: 4, start: 1),
  table.vline(x: 5, start: 1),
  table.vline(x: 6, start: 1),
  table.vline(x: 7, start: 1),
  table.vline(x: 8, start: 1),
  table.vline(x: 9, start: 1),
  table.vline(x: 10, start: 1),
  table.vline(x: 11, start: 1),
  table.vline(x: 12 ),
  table.vline(x: 13),
  [1], [B409], [```nasm mov ah, 09```], 
  // регистры AX, BX, CX, DX
  [0000], [0000], [0000], [0000],
  // регистр SP
  [FFFE],
  // регистры DS, SS, CS
  [489D], [489D], [489D],
  // регистр IP
  [0100],
  // флаги
  [00000010],
  table.hline(),
  [2], [BA3301], [```nasm mov dx, 0133```], 
  // регистры AX, BX, CX, DX
  [0900], [0000], [0000], [0000],
  // регистр SP
  [FFFE],
  // регистры DS, SS, CS
  [489D], [489D], [489D],
  // регистр IP
  [0102],
  // флаги
  [00000010],
  table.hline(),
  [3], [CD21], [```nasm int 21```], 
  // регистры AX, BX, CX, DX
  [0900], [0000], [0000], [0133],
  // регистр SP
  [FFFE],
  // регистры DS, SS, CS
  [489D], [489D], [489D],
  // регистр IP
  [0105],
  // флаги
  [00000010],
  table.hline(),
  [4], [E82100], [```nasm call 012B```], 
  // регистры AX, BX, CX, DX
  [0900], [0000], [0000], [0133],
  // регистр SP
  [FFFE],
  // регистры DS, SS, CS
  [489D], [489D], [489D],
  // регистр IP
  [0107],
  // флаги
  [00000010],
  table.hline(),
  [5], [BA4501], [```nasm mov dx, 0145```], 
  // регистры AX, BX, CX, DX
  [0900], [0000], [0000], [0133],
  // регистр SP
  [FFFC],
  // регистры DS, SS, CS
  [489D], [489D], [489D],
  // регистр IP
  [012B],
  // флаги
  [00000010],
  table.hline(),
  [6], [B409], [```nasm mov ah, 09```], 
  // регистры AX, BX, CX, DX
  [0900], [0000], [0000], [0147],
  // регистр SP
  [FFFC],
  // регистры DS, SS, CS
  [489D], [489D], [489D],
  // регистр IP
  [012E],
  // флаги
  [00000010],
  table.hline(),
  [7], [CD21], [```nasm int 21```], 
  // регистры AX, BX, CX, DX
  [0900], [0000], [0000], [0147],
  // регистр SP
  [FFFC],
  // регистры DS, SS, CS
  [489D], [489D], [489D],
  // регистр IP
  [0130],
  // флаги
  [00000010],
  table.hline(),
  [8], [C3], [```nasm ret```], 
  // регистры AX, BX, CX, DX
  [0900], [0000], [0000], [0147],
  // регистр SP
  [FFFC],
  // регистры DS, SS, CS
  [489D], [489D], [489D],
  // регистр IP
  [0132],
  // флаги
  [00000010],
  table.hline(),
  [9], [B80700], [```nasm mov ax, 0007```], 
  // регистры AX, BX, CX, DX
  [0900], [0000], [0000], [0147],
  // регистр SP
  [FFFE],
  // регистры DS, SS, CS
  [489D], [489D], [489D],
  // регистр IP
  [010A],
  // флаги
  [00000010],
  table.hline(),
  [10], [053000], [```nasm add ax, 0030```], 
  // регистры AX, BX, CX, DX
  [0007], [0000], [0000], [0147],
  // регистр SP
  [FFFE],
  // регистры DS, SS, CS
  [489D], [489D], [489D],
  // регистр IP
  [010D],
  // флаги
  [00000010],
  table.hline(),
  [11], [BB0500], [```nasm mov bx,0005```], 
  // регистры AX, BX, CX, DX
  [0037], [0000], [0000], [0147],
  // регистр SP
  [FFFE],
  // регистры DS, SS, CS
  [489D], [489D], [489D],
  // регистр IP
  [0110],
  // флаги
  [00000010],
  table.hline(),
  [12], [83C330], [```nasm add bx, 0030```], 
  // регистры AX, BX, CX, DX
  [0037], [0005], [0000], [0147],
  // регистр SP
  [FFFE],
  // регистры DS, SS, CS
  [489D], [489D], [489D],
  // регистр IP
  [0113],
  // флаги
  [00000010],
  table.hline(),
  [13], [8BD0], [```nasm mov dx, ax```], 
  // регистры AX, BX, CX, DX
  [0037], [0035], [0000], [0147],
  // регистр SP
  [FFFE],
  // регистры DS, SS, CS
  [489D], [489D], [489D],
  // регистр IP
  [0116],
  // флаги
  [00001010],
  table.hline(),
  [14], [B402], [```nasm mov ah, 02```], 
  // регистры AX, BX, CX, DX
  [0037], [0035], [0000], [0037],
  // регистр SP
  [FFFE],
  // регистры DS, SS, CS
  [489D], [489D], [489D],
  // регистр IP
  [0118],
  // флаги
  [00001010],
  table.hline(),
  [15], [CD21], [```nasm int 21```], 
  // регистры AX, BX, CX, DX
  [0237], [0035], [0000], [0037],
  // регистр SP
  [FFFE],
  // регистры DS, SS, CS
  [489D], [489D], [489D],
  // регистр IP
  [011A],
  // флаги
  [00001010],
  table.hline(),
  [16], [B200], [```nasm mov dl, 00```], 
  // регистры AX, BX, CX, DX
  [0237], [0035], [0000], [0037],
  // регистр SP
  [FFFE],
  // регистры DS, SS, CS
  [489D], [489D], [489D],
  // регистр IP
  [011C],
  // флаги
  [00001010],
  table.hline(),
  [17], [CD21], [```nasm int 21```], 
  // регистры AX, BX, CX, DX
  [0237], [0035], [0000], [0000],
  // регистр SP
  [FFFE],
  // регистры DS, SS, CS
  [489D], [489D], [489D],
  // регистр IP
  [011E],
  // флаги
  [00001010],
  table.hline(),
  [18], [8BD3], [```nasm mov dx, bx```], 
  // регистры AX, BX, CX, DX
  [0200], [0035], [0000], [0000],
  // регистр SP
  [FFFE],
  // регистры DS, SS, CS
  [489D], [489D], [489D],
  // регистр IP
  [0120],
  // флаги
  [00001010],
  table.hline(),
  [19], [B402], [```nasm mov ah, 02```], 
  // регистры AX, BX, CX, DX
  [0200], [0035], [0000], [0035],
  // регистр SP
  [FFFE],
  // регистры DS, SS, CS
  [489D], [489D], [489D],
  // регистр IP
  [0122],
  // флаги
  [00001010],
  table.hline(),
  [20], [CD21], [```nasm int 21```], 
  // регистры AX, BX, CX, DX
  [0200], [0035], [0000], [0035],
  // регистр SP
  [FFFE],
  // регистры DS, SS, CS
  [489D], [489D], [489D],
  // регистр IP
  [0124],
  // флаги
  [00001010],
  table.hline(),
  [21], [B8004C], [```nasm mov ax, 4C00```], 
  // регистры AX, BX, CX, DX
  [0235], [0035], [0000], [0035],
  // регистр SP
  [FFFE],
  // регистры DS, SS, CS
  [489D], [489D], [489D],
  // регистр IP
  [0126],
  // флаги
  [00001010],
  table.hline(),
  [22], [CD21], [```nasm int 21```], 
  // регистры AX, BX, CX, DX
  [4C00], [0035], [0000], [0035],
  // регистр SP
  [FFFE],
  // регистры DS, SS, CS
  [489D], [489D], [489D],
  // регистр IP
  [0129],
  // флаги
  [00001010],
  table.hline(),
  [23], [CD21], [```nasm int 21```], 
  // регистры AX, BX, CX, DX
  [0192], [2110], [F670], [0BB2],
  // регистр SP
  [0106],
  // регистры DS, SS, CS
  [2110], [0192], [0000],
  // регистр IP
  [0000],
  // флаги
  [10100011],
  table.hline(),
  

)

#set text( 
  size: 14pt
)

#text(size: 12pt, align(center)[Таблица трассировки первой программы])
